# Easy Python learning #
# Variables
```sh
# Assigning values to variables
name = "Alice"      # a string
age = 25            # an integer
height = 5.6        # a float (decimal number)
is_student = True   # a boolean (True or False)

# Using the variables
print(name)
print(age)
print(height)
print(is_student)
```
What’s happening here:
* name stores text (a string)
age stores a whole number (int)
height stores a decimal number (float)
is_student stores a True/False value (boolean)
If you want, I can also show:
how variables change
how to combine variables
or examples related to DevOps / automation 🚀
